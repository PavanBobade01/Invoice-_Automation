# Application Name
spring.application.name=InvoiceProcessingAutomation

# POSTGRESQL DATABASE CONFIGURATIONS
spring.datasource.url=jdbc:postgresql://localhost:5432/invoiceprocessingautomation
spring.datasource.username=postgres
spring.datasource.password=P@van01
spring.datasource.driver-class-name=org.postgresql.Driver

# HIBERNATE CONFIGURATIONS
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Disable Spring Security auto-configuration if not needed
#spring.security.enabled=false

# Optional: server port (default is 8080)
server.port=8080
